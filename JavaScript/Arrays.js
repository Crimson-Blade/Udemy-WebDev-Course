//Define and access arrays
let arr = [1, 2, 3, 4, 5];
let arr2 = ['Hello', 3, 5, 'Yes Pleaseee'];
console.log((arr2[3]).toUpperCase());
// Array Methods
// Push - Add to end
// Pop - Remove from end
// Shift - remove from start
// unshift - add to start
console.log("Array Methods");
const planets = ['The Moon', 'Venus', 'Earth', 'Mars', 'Jupiter'];
planets.shift();
planets.push("Saturn");
planets.unshift("Mercury");
console.log(planets);

// Arrays are similar to Object Literals

//2D arrays
const arr2D = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

